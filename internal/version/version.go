// Package version contains version information for the enumdns application.
package version

// Version contains the current version of enumdns.
var (
	Version = "dev"

	// GitHash contains the git commit hash at build time.
	GitHash = "dev"
	// GoBuildEnv contains the Go build environment information.
	GoBuildEnv = "dev"
	// GoBuildTime contains the build timestamp.
	GoBuildTime = "dev"
)
